# Scoring MCP Blueprint

Local tools:

- `scoring.calculateTrapScore`
- `scoring.explainTrapScore`
- `scoring.compareSnapshots`
- `scoring.generateAlertText`

This server should import `packages/scoring` and never call external services.
